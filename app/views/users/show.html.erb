<%= render 'profile_box', user: @user %>

<div class="six">
  <%= simple_form_for [current_user, @status] do |f| %>
  <%= f.input :text %>
  <%= f.button :submit %>
<% end %>
</div>

 <p>Hometown: <%= @user.hometown %></p>


<div class="tall">
<% @user.statuses.each do |status| %>
  <p><%= link_to status.text, user_status_path(@user, status) %></p>
<% end %>
</div>

<h3>Following:</h3>
<div class="friends">
  <% @user.friendships.each do |friendship| %>
  <p><%= link_to friendship.friend.name, friendship.friend %> | <%= link_to "Remove", friendship, method: :delete %></p>
<% end %>

<h3>Followers:</h3>
<div class="inverse_friends">
  <% @user.inverse_friendships.each do |added_by| %>
  <p><%= added_by.user.name %></p>
<% end %>
</div>
